apiVersion: v1
kind: Secret
type: kubernetes.io/service-account-token
metadata:
  name: workaround-admin-token
  namespace: kube-system
  annotations:
    kubernetes.io/service-account.name: workaround-admin
